## 第1题 代数证明

### 已知条件

设一元线性回归总体模型为：

$$y_i = \beta_0 + \beta_1 x_i + \varepsilon_i \quad (i = 1, 2, \dots, n)$$

其中 $\varepsilon_i$ 为随机误差项。样本回归模型的拟合值与残差定义为：

$$\hat{y}_i = \hat{\beta}_0 + \hat{\beta}_1 x_i, \qquad e_i = y_i - \hat{y}_i = y_i - \hat{\beta}_0 - \hat{\beta}_1 x_i$$

最小二乘法（OLS）的目标是最小化残差平方和：

$$Q(\hat{\beta}_0, \hat{\beta}_1) = \sum_{i=1}^{n} e_i^2 = \sum_{i=1}^{n} (y_i - \hat{\beta}_0 - \hat{\beta}_1 x_i)^2$$

---

### 证明一：残差之和为零 $\displaystyle \sum_{i=1}^{n} e_i = 0$

对 $\hat{\beta}_0$ 求偏导并令其等于 0（极值条件）：

$$\frac{\partial Q}{\partial \hat{\beta}_0} = \sum_{i=1}^{n} 2 \cdot (y_i - \hat{\beta}_0 - \hat{\beta}_1 x_i) \cdot (-1) = 0$$

两边除以 $-2$，得：

$$\sum_{i=1}^{n} (y_i - \hat{\beta}_0 - \hat{\beta}_1 x_i) = 0$$

注意到括号内正是残差 $e_i$，因此：

$$\sum_{i=1}^{n} e_i = 0 \quad \blacksquare$$

> OLS 回归线必然经过样本均值点 $(\bar{x}, \bar{y})$。由上式可推出 $\bar{e} = \frac{1}{n}\sum e_i = 0$，即残差的样本均值为零。

---

### 证明二：残差与自变量乘积之和为零 $\displaystyle \sum_{i=1}^{n} x_i e_i = 0$

对 $\hat{\beta}_1$ 求偏导并令其等于 0（极值条件）：

$$\frac{\partial Q}{\partial \hat{\beta}_1} = \sum_{i=1}^{n} 2 \cdot (y_i - \hat{\beta}_0 - \hat{\beta}_1 x_i) \cdot (-x_i) = 0$$

两边除以 $-2$，得：

$$\sum_{i=1}^{n} x_i \cdot (y_i - \hat{\beta}_0 - \hat{\beta}_1 x_i) = 0$$

括号内仍是残差 $e_i$，因此：

$$\sum_{i=1}^{n} x_i e_i = 0 \quad \blacksquare$$

---

## 第2题 理解思考题

### 问题

为什么在多元回归中，往模型中添加一个纯随机生成的无关特征，$R^2$ 会上升或保持不变，而 $R^2_{\text{adj}}$ 会下降？

---

### 一、为什么 $R^2$ 只会上升或保持不变

$R^2$ 的定义为：

$$R^2 = 1 - \frac{RSS}{TSS}$$

其中总离差平方和 $TSS = \sum (y_i - \bar{y})^2$ 只与因变量 $y$ 有关，**与自变量个数无关，是一个固定常数**。

关键在于：**OLS 是在一个不断扩大的参数空间中寻找最小值**。

- 原模型有 $k$ 个自变量，参数空间为 $\mathbb{R}^{k+1}$，得到最小残差平方和 $RSS_{\text{old}}$。
- 加入一个新变量后，模型有 $k+1$ 个自变量，参数空间扩大为 $\mathbb{R}^{k+2}$。

在更大的参数空间中做最小化，找到的最小值 **不可能比原来更大**（因为原最优解仍然是新空间中的一个候选点）。因此：

$$RSS_{\text{new}} \le RSS_{\text{old}} \quad \Longrightarrow \quad R^2_{\text{new}} \ge R^2_{\text{old}}$$

即使新变量是纯随机噪声，它也可能在样本中偶然地"撞上"一些残差波动，使 $RSS$ 略微减小。只有当新变量的回归系数估计值恰好为 0 时（概率极低），$RSS$ 才不变、$R^2$ 才持平。

> **结论**：$R^2$ 是一个"只升不降"的指标，它惩罚不了无效变量，天然鼓励过拟合。

---

### 二、为什么 $R^2_{\text{adj}}$ 会下降

调整 $R^2$ 的定义为：

$$R^2_{\text{adj}} = 1 - \frac{RSS/(n-k-1)}{TSS/(n-1)}$$

其中 $n$ 为样本量，$k$ 为自变量个数。它的核心改动是用 **均方误差** 代替了平方和，并引入了 **自由度** 的修正。

#### 1. 自由度视角

- 分子自由度：残差自由度 $df_{\text{res}} = n - k - 1$
- 分母自由度：总自由度 $df_{\text{tot}} = n - 1$（固定不变）

每加入一个自变量，残差自由度就 **减少 1**（少了一个"灵活度"去解释噪声）。

#### 2. RSS 与自由度的此消彼长

加入新变量后：

- **分子上的 $RSS$**：会减小（即使是随机变量，也多少拟合了一点噪声）。
- **分母上的 $n - k - 1$**：也会减小（自由度损失）。

$R^2_{\text{adj}}$ 本质上比较的是 $\dfrac{RSS}{n-k-1}$ 这个比值。对于一个**纯随机无关变量**，它对 $RSS$ 的削减幅度通常很小，不足以补偿自由度损失带来的分母缩小效应，因此：

$$\frac{RSS_{\text{new}}}{n-(k+1)-1} > \frac{RSS_{\text{old}}}{n-k-1}$$

从而 $R^2_{\text{adj}}$ 下降。


---

### 三、对比总结

| 指标 | 公式核心 | 对新变量的态度 | 纯随机变量加入后的表现 |
| :--- | :--- | :--- | :--- |
| $R^2$ | $1 - RSS/TSS$ | 只看平方和，不惩罚变量个数 | 只升不降（最多持平） |
| $R^2_{\text{adj}}$ | $1 - \dfrac{RSS/(n-k-1)}{TSS/(n-1)}$ | 用自由度惩罚无效变量 | 通常下降，只有真有解释力才上升 |

**本质区别**：$R^2$ 度量的是"样本内拟合了多少"，而 $R^2_{\text{adj}}$ 度量的是"每消耗一个自由度，真正解释了多少"。后者因此成为判断新增变量是否值得保留的经验准则——只有当 $R^2_{\text{adj}}$ 上升时，才说明这个变量是真的在提供信息，而不是单纯吃掉了自由度。